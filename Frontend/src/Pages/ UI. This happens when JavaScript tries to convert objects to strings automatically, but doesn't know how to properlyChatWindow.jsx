useEffect(() => { display the object's contents
  // Join personal room for receiving messages
  if.

This is a common issue when trying to log or (loggedInUserEmail) {
     display objects directly. Letconsole.log("Joining room for me help you fix this problem user:", loggedInUserEmail);
    :

## Whysocket.emit("joinRoom", loggedIn You're Seeing [UserEmail);
  object Object]

When you try to log an object directly}
  
  const onConnect = () => set or concatenate it with a string, JavaScript callsSocketConnected(true);
  const onDisconnect = () => the object's `toString()` setSocketConnected( method, which by default returns `[objectfalse);

  socket.on("connect", onConnect);
  socket.on("disconnect", onDisconnect);

   Object]`. This doesn't show you the actual contents of thereturn () => { object.

## How
    socket.off("connect", onConnect); to Fix It

### 
    socket.off("disconnect", onDis1. Inconnect);
  };
}, [log Console Logs

If you're trying to log objectsgedInUserEmail]);